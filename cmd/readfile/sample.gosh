#!/usr/bin/env gosh

&&!=:=()...

# thsi is a commenT

^ := { # another comment

)
]
}

${ ls -l }
$${ echo * }

if alpha == 23 {
    x := 8383.8
    y := -23    # why???
    x := +33;print(foo)
    print("hello, world\n")
    return blop(a,b,c)
}

break
return
else
func
switch

${ ls -l }
$${ df -h }
   $   { ps auxw }
$$ {} # erg
$ ouch # goo
$$$
